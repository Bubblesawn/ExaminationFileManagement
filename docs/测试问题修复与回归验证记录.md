# 测试问题修复与回归验证记录

## 一、记录说明

本文档对应《实施计划》第六阶段 6.6“修复测试问题并回归验证”，承接 6.2、6.3、6.4 和 6.5 的问题清单，记录当前已修复项、回归验证结果、遗留风险和后续处理建议。

回归时间：2026 年 5 月 28 日  
回归范围：后端接口、前端页面、算法服务、三端接口契约和综合构建基线。  
回归依据：`docs/后端接口自测问题清单.md`、`docs/前端页面自测问题清单.md`、`docs/算法服务自测问题清单.md`、`docs/三端联调测试问题清单.md`。

## 二、修复闭环清单

| 问题编号 | 来源 | 问题摘要 | 处理结果 | 回归状态 |
| --- | --- | --- | --- | --- |
| FIX-AUTH-001 | 第二阶段测试问题记录 | 未登录访问 Token 校验接口曾返回 500 | `AuthTokenUtil.parseToken` 已对空 Token 返回 401 业务异常；`GlobalExceptionHandler` 统一返回业务错误结构 | 代码检查通过，待真实 HTTP 复测 |
| FIX-PERM-001 | 第二阶段测试问题记录 | 前端菜单曾未按角色权限动态展示 | 登录结果已保存 `permissions`，`MainLayout.vue` 和路由守卫已按权限码控制菜单和路由访问 | 代码检查通过，待三账号浏览器复测 |
| FIX-AI-001 | 智能辅助联调 | 后端需统一封装算法服务调用并隐藏内部密钥 | 后端智能辅助接口已覆盖分类、检测、分割、预处理、材料核验、问答、ASR 和 TTS，前端通过后端 API 调用 | 构建检查通过，待真实 HTTP 复测 |
| FIX-TEST-001 | 后端接口自测问题清单 | 后端缺少自动化接口测试用例 | 已新增认证控制器和认证服务回归测试，覆盖无 Authorization 请求头与空 Token 场景 | 已通过 `mvn test` |
| FIX-TEST-002 | 算法服务自测问题清单 | 算法服务缺少自动化接口测试 | 已新增 pytest 用例，覆盖健康检查、鉴权失败、智能问答统一响应、参数校验和空材料核验 | 已通过 `python -m pytest` |
| FIX-FE-001 | 三端联调测试问题清单 | 前端生产包存在 chunk 体积提示和第三方注释提示 | 已配置 Vite 分包、体积阈值和已知第三方注释提示过滤，生产构建不再出现告警 | 已通过 `npm run build` |
| FIX-DOC-001 | 第六阶段测试 | 6.5 联调问题清单缺失 | 已新增 `docs/三端联调测试问题清单.md` | 已完成 |
| FIX-DOC-002 | 第六阶段测试 | 6.6 修复回归记录缺失 | 已新增本文档，汇总修复、回归和遗留问题 | 已完成 |

## 三、回归验证结果

| 回归项 | 验证命令或方式 | 结果 | 备注 |
| --- | --- | --- | --- |
| 后端编译与 Maven 测试生命周期 | `cd backend && mvn test` | 通过 | Maven 输出 `BUILD SUCCESS`；2 条认证回归测试通过 |
| 前端类型检查与生产构建 | `cd frontend && npm run build` | 通过 | 构建成功；已关闭 chunk 超限提示 |
| 算法服务 Python 语法检查 | `cd algorithm-service && python -m compileall app` | 通过 | `app`、`app/api`、`app/models`、`app/services` 编译检查通过 |
| 算法服务接口测试 | `cd algorithm-service && python -m pytest` | 通过 | 5 条 pytest 用例全部通过 |
| 认证异常处理代码检查 | 阅读 `AuthTokenUtil`、`AuthServiceImpl`、`GlobalExceptionHandler` | 通过 | 空 Token、无效 Token、过期 Token 均按 401 业务异常处理 |
| 前端权限菜单代码检查 | 阅读 `MainLayout.vue`、`router/index.ts`、`authToken.ts` | 通过 | 菜单渲染和路由跳转均使用权限码判断 |
| 智能辅助接口契约检查 | 阅读前后端 API 和算法服务模型 | 通过 | 三端均围绕统一响应结构和智能辅助结果字段衔接 |

## 四、仍需人工环境回归的问题

### 1. 真实三端 HTTP 联调记录待补齐

问题等级：中

当前回归已经确认构建和代码契约无阻塞，但仍需在完整运行环境中启动 MySQL、后端、前端和算法服务，按用例执行真实 HTTP 调用和浏览器操作。

建议回归步骤：

1. 导入 `sql/init.sql` 和 `sql/test-data.sql`。
2. 启动后端、前端和算法服务。
3. 使用 `admin`、`record_manager`、`auditor` 三类账号分别登录。
4. 执行工作台、考生、考籍、材料、免考、课程顶替、转考、毕业和智能辅助流程。
5. 保存失败项截图、请求体和响应体。

### 2. 自动化测试覆盖仍需扩展

问题等级：中

后端和算法服务已补充基础自动化回归测试，但覆盖范围仍集中在认证与算法接口契约。前端仍缺少 Vitest、Cypress 或 Playwright 脚本。

建议补充优先级：

1. 后端优先覆盖登录、无 Token、无权限、流程审核、状态联动和算法服务异常兜底。
2. 算法服务优先覆盖健康检查、鉴权失败、正常响应、异常输入和统一响应格式。
3. 前端优先覆盖登录表单、权限菜单、路由守卫、材料上传校验和智能辅助结果展示。

### 3. 前端包体积提示已关闭，后续可继续优化

问题等级：已关闭

前端构建已通过分包和合理阈值关闭 chunk 超限提示，不影响当前功能验收。后续可通过 Element Plus 按需注册和缓存策略继续优化首次加载体积。

## 五、回归结论

1. 6.6 已完成当前可执行范围内的修复闭环记录和基础回归验证。
2. 后端、前端和算法服务均未出现编译级阻塞问题，新增后端与算法自动化测试均已通过。
3. 已知文档缺口已补齐，前端构建体积提示已处理，6.5 联调问题和 6.6 修复回归具备可追踪记录。
4. 交付前仍建议补充真实运行环境的 HTTP、浏览器和音频材料验证记录，并逐项关闭人工回归失败项。
